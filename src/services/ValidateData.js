export default class ValidateData{
    static checkChevron(str) {

        return str;  
    }

    static checkIfEmpty(str) {
        return str === "";
    }

    static dd(params) {
        console.log(params)
        alert()
    }
}